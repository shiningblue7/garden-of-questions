# A Garden of Questions

A small static web experience made for Netlify. It uses plain HTML, CSS, and JavaScript, so the publish directory is the project root.

## Publish

On Netlify, set:

- Build command: leave blank
- Publish directory: `.`

The generated visual asset lives at `assets/garden-of-questions.png`.
